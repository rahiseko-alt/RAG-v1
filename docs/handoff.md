# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。
過去の失敗の蓄積は `docs/failures.md`（append-only・消さない）を見る。

## ①今回実施

2体のサブエージェントに「無意味な記述が無いか」を敵対的に確認させ、指摘を反映してAGENTS.md等を
整理した。主な変更：

- **廃止済みのツリー機構への言及を除去**：`AGENTS.md`「進捗管理は軽量」節、
  `.claude/skills/checkin-checkout/SKILL.md` 冒頭にあった「原子分解ツリーのような機械強制の
  進捗管理は行わない」等の、過去の仕組みをわざわざ引き合いに出す説明文を削除し、今の運用だけを
  簡潔に書くよう修正した。
- **重複の統合**：「CIが緑であることだけを守る」という文言がAGENTS.md内2箇所＋SKILL.md1箇所で
  ほぼ逐語反復していたのを1箇所（検証の規律）に集約。「PR instructions」節は「検証の規律」
  「セッション開始/終了の儀式」の内容を再パッケージしただけだったため節ごと削除し、唯一新しい
  情報だった「1コミット＝1目的」は普遍ルールの既存箇条書きに統合した。
- **手順0内の重複警告を統合**：「Private では branch protection が強制されない」という同じ警告が
  手順0-aと手順0-bの⚠で2回書かれていたのを、後者を前者への参照に短縮。
- **presets/_TEMPLATE.mdの機械強制手順を修正**：AGENTS.mdの現行推奨（Rulesets）と食い違う旧来の
  Branch protection手順が個別に書かれていたのを、AGENTS.md手順0への参照に一本化（重複かつ将来また
  食い違う=陳腐化リスクを解消）。
- 「配下ごとの調整」節の継承ルール重複、Testing instructionsの「echo禁止」重複も解消。

## ②今回トラブル

無し。

## ③次回やる事

特になし（区切りの良いところまで完了）。
