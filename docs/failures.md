# failures（失敗の蓄積ログ / append-only・消さない）

同じ失敗を繰り返さないための**蓄積型**ログ。handoff（`docs/handoff.md`・毎回上書き）とは
役割が違い、ここは**消さずに積む**。1件＝**日付＋事象＋根因＋教訓**。

---

## 2026-08-01 チェックインで `main` しか見ず「作業なし」と報告した

- **事象**：セッション開始時、`docs/handoff.md` と `git log` を見て「コミットは Initial commit の
  1件のみ、前回は区切りよく完了、残課題なし」とユーザーに報告した。実際にはリモートに
  `codex/public-delivery-workbench` があり、そこに 13 コミットの本体開発が丸ごと存在していた。
  ユーザーの「既にブランチに何かあるだろ」で発覚した。
- **根因**：`git log` はカレントブランチしか見ない。`git fetch` も `git branch -r` も実行せず、
  ローカルの可視範囲だけで「リポジトリ全体の状態」を結論づけた。handoff がテンプレ整理の話しか
  書いていなかったため、それが全体像だと思い込んだ（handoff は揮発層であり、全体像ではない）。
- **教訓**：**チェックインでは必ず `git fetch --all --prune` してから `git branch -r` で
  全ブランチを見る。** 「コミットが1件しかない」は「作業が無い」ではなく「見ている場所が違う」を
  まず疑う。handoff の記述と、リポジトリの実体が食い違うことがある。

## 2026-08-01 この環境の git プロキシは指定ブランチ以外への push を拒否する

- **事象**：medguide-rag 取り込みにあたり、旧ブランチの履歴を `archive/codex-public-delivery-workbench`
  タグで凍結しようとしたが `error: RPC failed; HTTP 403` で拒否された。マージ後に旧ブランチを
  `git push origin :codex/public-delivery-workbench` で削除しようとしたが、同様に拒否された。
- **根因**：Claude Code の remote 実行環境では git アクセスがローカルプロキシ
  （`http://local_proxy@127.0.0.1:<port>/git/...`）経由になっており、**セッションに指定された
  作業ブランチへの push 以外を通さない**。タグ ref もブランチ削除もこれに該当する。
  GitHub MCP にもブランチ削除ツールは無い。
- **教訓**：**この環境では「タグを打つ」「ブランチを消す」は AI 側で完結できない。**
  履歴の保全をタグに依存する計画を立てない。今回は `--allow-unrelated-histories` の
  マージコミットにより 13 コミットが `main` の祖先になったため、タグが無くても履歴は保全された
  （逆に言えば、squash マージしていたら復旧不能だった）。ブランチ削除等はユーザーへの
  手動作業として引き継ぐこと。

## 2026-08-01 移行計画に、実行すれば確実に失敗する誤りを複数含んだまま承認を求めた

- **事象**：medguide-rag をこのリポジトリへ持ち込む計画を立て、ユーザーの了承を得る直前まで進めた。
  ユーザー指示で 3 視点の敵対検証サブエージェントにかけたところ、実行すれば確実に失敗する誤りが
  複数見つかった。主なもの：(1) CI に `uv sync` と書いたが、これでは `ruff`/`pytest` が入らない
  （`--extra dev` が必要）、(2) それを直すと e2e テストの `skipif` が外れて別の赤が出る、
  (3) `ruff` の赤は 3 件だと記録を鵜呑みにしたが実測 10 件、(4) `auto-merge.yml` が
  `--squash --delete-branch` を自動実行するため「13 コミットの履歴を保存する」という計画の目的を
  自分で破壊する、(5) Dependabot PR 8 本を「全て Next.js 関連」と誤認（4 本は github-actions で、
  うち 2 本は移行後も有効）。
- **根因**：既存ドキュメント（`roadmap-state.json` の `known_verified` 等）の数字を実測せずに
  引用した。ワークフローの中身（`auto-merge.yml` が何をするか）を読まずに「変えないファイル」に
  分類した。PR 一覧はタイトルだけ見てエコシステムを確認しなかった。
- **教訓**：**計画に書く数字と前提は、引用ではなく自分で実測する。** ツールは実際に入れて走らせる
  （`ruff` / `mypy` は軽量なので本体依存なしで検証できた）。「変えないファイル」に分類する前に、
  そのファイルが何をするか読む。記録された `known_verified` は過去の測定値であって現在の状態ではない。

## 2026-08-01 指示されていないチェックアウトを自己判断で開始し、コードPRと引継ぎPRを分けてしまった

- **事象**：ユーザーから「次いけ」（次の作業へ進め）とだけ指示された後、`coverage-loop` の
  evidence metadata・台帳永続化（PR #19）を実装してマージした直後、指示されていないのに
  「区切りが良い」と自己判断で `docs/handoff.md` / `memory.md` の更新を開始し、コード変更（PR #19）
  とは**別の新しいブランチ状態・別PR（PR #20）**を作ってしまった。ユーザーから「マージ→チェックアウト
  は順序が逆。チェックアウトの一環としてマージがある」「チェックアウトは指示無しで発火するな」と
  指摘を受けた。
- **根因**：(1) `checkin-checkout` スキルの当時の文面「意味のある作業を終えた、または区切りをつける
  タイミングで実行する」を、ユーザー指示が無くても自己判断で発火してよい根拠として読んでしまった。
  (2) スキル手順5「既にPRがある場合は、今回のpushがそのPRに乗ったことを確認する」を守らず、
  未マージのPR #19に引継ぎ更新を追い pushする代わりに、先にPR #19を単体でマージしてしまった。
- **教訓**：**チェックアウト（handoff編集・commit・push・PR・mainへのマージという一連の操作）は、
  ユーザーがその全範囲を明示的に指示した時にのみ発火する。** 「区切りが良さそう」はAI単独の
  発火理由にならない（`AGENTS.md` と `checkin-checkout` スキルを修正済み・PR #20）。範囲を絞った
  指示（例：「引継ぎ書いて」）は編集だけで止め、commit・push・PR・マージへ勝手に広げない。
  本題のコード変更に対応するPRがまだ開いている間は、引継ぎ更新はそのPRに追い pushして
  1回のマージで完結させる——新しいPRを別に作らない。

## 2026-08-02 実験の入力（30問セット）を版管理せず、レポートの要約だけ残して失った

- **事象**：前回セッションが C-1/C-2/C-3 の3役で生成した30問が、リポジトリのどこにも
  保存されていなかった。`docs/session-reports/2026-08-01-coverage-loop-design.md` に残っていたのは
  役割ごとの件数表（各10問）と、別の10問ループの結果表だけで、**設問本体は失われていた**。
  今回 項目6（30問をA/B/Dへ流す）に着手して初めて発覚し、作り直しになった。
- **根因**：生成物を「セッションレポートに書いた＝記録した」と扱った。設問セットは実験の
  **入力**であり再実行に必須のデータなのに、成果物ではなく経過として扱っていた。
- **教訓**：**実験の入力（設問セット・固定パラメータ）は版管理下のファイルに置く。**
  レポートへの要約は記録の代わりにならない。特に before/after 比較を行う設計では
  **固定評価セット**が前提であり、毎回生成し直すと両側が比較不能になる（設計レポート
  「before/after比較は固定コーパス版、固定設定、固定評価セットで行う」）。今回は
  `data/eval/coverage-loop-30-questions.json` として版管理下に置き直した。

## 2026-08-02 0始まりIDに `or` のデフォルト値を使い、先頭チャンクの locator を落とした

- **事象**：`retrieved_chunks_from_sources()` で `str(source.get("chunk_id") or "")` と書いた。
  `chunk_id` は `src/ingest` で 0 始まりの連番なので、`chunk_id=0` が空文字になり、
  **各文書の先頭チャンク——検索で最も頻繁に surface される位置——の locator が常に失われていた**。
  自分で書いたテストは通っていた（0 を含むケースを書いていなかったため）。CodeRabbit の
  レビュー指摘で発覚。
- **根因**：「未設定なら空文字」を意図して `or` を使ったが、`or` は `0` も falsy として扱う。
  ID の値域（0始まりか1始まりか）を確認せずに書いた。テストの入力値も自分の想定内だけで作った。
- **教訓**：**`or` によるデフォルト値は、値域に `0` / `""` / `False` を含みうるフィールドに
  使わない。`is None` で明示的に判定する。** ID・カウント・スコアは特に危ない。
  テストには境界値（`0`、および `bool` は `int` の派生である点）を必ず入れる。

## 2026-08-02 証跡から言えない結論をD向けプロンプトに書き、taxonomy の目的を自ら破った

- **事象**：項目7でB回答に取得証跡を載せた際、D向けガイダンスに「surfaced chunk に必要な事実が
  無ければ `missing_knowledge` か `chunking_failure` を示唆する」と書いた。しかし surfaced chunk に
  無いことは「コーパスに無い（missing_knowledge）」「コーパスにはあるが検索が出せなかった
  （retrieval_failure）」「分割で分断された（chunking_failure）」の**いずれとも等しく整合**し、
  D はコーパスを渡されていないため区別できない。この taxonomy は「BがAに負けた→ナレッジ追加」
  という飛躍を防ぐために作ったのに、**同じ形の飛躍を自分で持ち込んだ**。あわせて
  `RetrievedChunk` の docstring が `retrieval_failure` を `generation_failure` の定義文で
  説明していた（PR #19 由来の誤りを検証せず引き継いだ）。CodeRabbit のレビュー指摘で発覚。
- **根因**：証跡を追加したことで「証跡があれば原因が分かる」と短絡した。**その証跡が何を証明し、
  何を証明しないか**を追加時に切り分けなかった。既存 docstring の文言を、正しさを確認せずに
  引き継いだ。
- **教訓**：**証拠を足したら「これで何が言えて、何が言えないか」を明示的に書く。** 今回の取得証跡が
  積極的に証明できるのは一方向だけ（surfaced chunk が事実を含むのに B が失敗 → `generation_failure`）。
  言えない側は `needs_quarantine` に落とす。**既存のコメント・docstring を引き継ぐときは、
  正しさを確認してから引き継ぐ**（誤りはコピーで増殖する）。

## 2026-08-02 `uv run` が dev 依存を venv から外し、PATH 上の別ツールが黙って代役した

- **事象**：`uv sync --locked --extra dev` の後に `uv run python -m scripts.…` を実行したところ、
  uv が既定環境（extras 無し）へ同期し直し、**`ruff` / `mypy` / `pytest` を venv から削除**した。
  その状態で `uv run pytest -q` を叩くと、venv に pytest が無いため PATH 上の
  `/root/.local/bin/pytest`（システムの Python 3.11）が実行され、`ModuleNotFoundError:
  No module named 'fastapi'` などの収集エラーが9件出た。同時に `uv run mypy src` も別バージョンで
  走り、**本来 0 件のはずの `unused-ignore` が2件出た**。`src/rag/__init__.py` の
  `type: ignore` を「不要になったから消す」と判断しかけたが、実際にはコードは何も壊れていなかった。
- **根因（未特定）**：当初ここに「`uv run <cmd>` は `--extra` を指定しないと既定環境へ同期し直す」と
  断定して書いたが、**独立検証で再現できなかった**（uv 0.8.17 で `uv sync --locked --extra dev` の後に
  `uv run ruff` / `uv run mypy src` / `uv run pytest -q` / `uv run python -m scripts.…` を順に叩いても、
  `.venv/bin` に3ツールとも残り続けた）。**事象は実際に起きたが、根因は未特定である。**
  確かなのは次の2点だけ：(a) ある時点で venv から dev ツールが消えていた、(b) PATH 上の
  `/root/.local/bin` に同名ツール（システム Python 3.11）があったため、**コマンドは失敗せず
  『違う環境で成功／失敗』した**。エラーが「依存が無い」ではなく「テストが壊れた」
  「型注釈が不要になった」の形で現れ、原因の切り分けを誤らせた。
- **教訓**：**品質チェックは常に `uv run --extra dev <cmd>` で叩く**（`uv sync` 側だけでは足りない。
  根因が未特定でも、これで事象は回避できる）。**そして、再現手順を確かめないまま根因を断定して
  失敗ログに書かない**——この項目自体が、確かめずに書いた根因を独立検証で否定された実例である。
  そして**チェックが想定外の結果を出したら、まず実行主体（インタプリタとツールの実体パス）を疑う**。
  今回は traceback の `/usr/lib/python3.11/` と `uv run which pytest` の
  `/root/.local/bin/pytest` が決め手だった。**PATH 上に同名ツールがある環境では、
  「動いた」ことは「正しい環境で動いた」ことを意味しない。**

## 2026-08-02 語一致プローブの「不在」を強い証拠として扱わせ、正しかった判定を2件壊した

- **事象**：D が原因を分類できず30問中24問が隔離になった問題に対し、コーパス全体を走査して
  **語の在不在**を報告する `corpus_probe` を実装し、D に「`absent_from_corpus` があれば
  `missing_knowledge` を主張してよい」と指示した。隔離は 24件 → 0件になったが、コーパスを直接
  読める独立検証者に8件を敵対検証させたところ、**2件（C3-01・C2-10）が誤判定**だった。
  **【訂正】** 当初ここに「その2件はプローブ導入前は正しく `generation_failure` と判定できていた」と
  書いたが誤りで、独立検証と一次データで否定された。正しくは **C3-01 のみがプローブ前に
  `generation_failure`** であり、**C2-10 はプローブ前も `needs_quarantine`**（＝プローブは
  保留すべき件を断定させた）。さらに検証は**もう1件の巻き戻し（C1-05：`generation_failure` →
  `invalid_A`）**を発見しており、プローブ前に2件あった `generation_failure` は**0件に全滅**していた。
  例：C3-01 で D が「コーパスに無い」と断じた内容は、実際には surface 済みチャンク22に
  「結界を閉じない領域として語られることが多く」と**言い換えで書かれており、B に渡っていた**。
- **根因**：2つある。
  1. **語一致は言い換えを検出できない**のに、その「不在」を D への指示で*強い証拠*と呼んだ。
     プローブの限界を実装者（自分）は分かっていたのに、**D 向けの文言に落とすときに落とした**。
  2. **証拠の優先順位を決めなかった。** `generation_failure` は取得証跡が*積極的に証明できる
     唯一の方向*（`memory.md` [importance:H]）なのに、後から足した弱い証拠がそれを上書きできる
     形で並べてしまった。D が見られるのは各チャンク先頭240字のスニペットだけで、
     **強い証拠を確認する手段を持たないまま弱い証拠だけ増やした**のが実態。
- **教訓**：**証拠を足すときは「既存の強い証拠より下に置く」ことを明示し、順序を指示に書く。**
  弱い証拠を足すと、強い証拠が見えていない場所では弱い証拠が勝ってしまう。
  また、**プローブ等の近似手法は、その限界（今回は言い換え非検出）を利用者向けの文言に必ず
  書き写す**こと。実装者が知っていることは、指示に書かなければ利用者は知らない。
  加えて、**「隔離0件」のような極端な改善は疑って検証にかける**。今回は疑って正解だった。

## 2026-08-02 事実誤認を4箇所に伝播させた（自分で書いた「誤りはコピーで増殖する」を再び踏んだ）

- **事象**：プローブ導入前後の比較で「誤判定2件は、どちらもプローブ導入前は正しく `generation_failure`
  と判定できていた」と書いた。実際には**1件（C3-01）だけが正しく判定できていた**もので、
  もう1件（C2-10）は**プローブ導入前から `needs_quarantine`（保留）**だった。当時の判定理由には
  「理由そのものを明示した文ではないため generation_failure と断定はできない」と自ら書かれていた。
  この誤った一文を、**レポート・`docs/failures.md`・コミットメッセージ・`src/coverage_loop.py` の
  docstring の4箇所にコピーして伝播**させた。独立検証エージェントが一次データ（`summary.json`）を
  数え直して発覚した。
- **根因**：2件を「まとめて」記述する際、片方（C3-01）の確認だけで満足し、もう片方（C2-10）の
  プローブ導入前の状態を**一次データに戻って確認しなかった**。一度文章にした瞬間に、それを他の
  ファイルへ複製する作業が先に進んでしまった。
- **教訓**：**複数件をまとめて主張するときは、全件を個別に一次データで確認してから書く。**
  1件確認しただけで「両方とも」「いずれも」と一般化しない。そして、**同じ主張を複数ファイルに
  コピーする前に、その主張自体を疑う一呼吸を置く**——このリポジトリの `docs/failures.md` には
  既に「誤りはコピーで増殖する」という教訓（2026-08-02、証跡プロンプトの件）が書かれていたのに、
  同じセッション内で再び同じ失敗をした。**教訓は書いただけでは効かない。次の一文を書く前に
  過去の教訓を読み返す一手間が要る。**

## 2026-08-02 評価セットが難問だけで構成され、対照群も答えの無い問いも0%だった

- **事象**：30問の評価セット（`data/eval/coverage-loop-30-questions.json`）が、全問「深い因果・条件・
  例外」を問う難問のみで構成されていた。判定不能が0件になった後の弱点分類表で、C-2・C-3の2つの
  問い型が**10問中10問とも同一分類（`missing_knowledge`）に飽和**し、「その問い型が弱点」なのか
  「難度が高すぎてどんな資料でも落ちる」のかを**区別できない状態**のまま先へ進みかけた。
  ユーザーから「質問がマニアックすぎる。検証としてやり方がおかしい」と指摘され、初めて気づいた。
- **根因**：設問生成の指示（C役）に「因果」「条件/例外」という難度の高い軸しか与えず、
  対照群（答えが確実にある易しい問い）・答えの無い問い・誤前提の問いを最初から作らなかった。
  難易度を検証するという発想自体が抜けていた。項目反応理論では「難度を極端に振った評価セットは
  そもそも差を測る能力（弁別力）を持たない」ことが知られており（フロア効果）、これは目新しい
  問題ではなく確立された落とし穴だった。
- **教訓**：**評価セットを作るときは、必ず対照群（易しい問い）と答えの無い問いを含める。**
  難問だけを集めると、結果が飽和して「弱点が見つかった」のか「測定できていない」のかが
  区別できなくなる。実在のQA/RAGベンチマーク（SQuAD 2.0・CRAG・Natural Questions等）は
  いずれも易しい問いを最頻に置き、答えの無い問いを11.8%〜51%含む——**自作の評価セットが
  先行研究の構成と極端に違う場合は、その違いに理由があるか自問すること。**

## 2026-08-03 分類器の測定器（ゴールドセット生成）自体に、測定対象と同じ種類の欠陥を持ち込んだ

- **事象**：D役分類器を検証するための構成的ゴールドセット（`data/eval/classifier-gold-set-v1.json`）
  を作り、コーパスを読める独立エージェント2体（確認側・反証側）に敵対検証させたところ、確認側は
  28件全件AGREEだったが、反証側は**28件中12件（43%）に実証的な反論**を発見した。根本原因は2つ：
  1. `retrieval_failure`/`chunking_failure`の構成で「answer_spanがkeptチャンクに含まれない」判定を
     文字列完全一致のみで行っており、言い換え（例：「渋谷事変編は大きな転換点。五条悟の封印」に対し
     「渋谷事変は五条封印と大量の被害」という別チャンクの言い換え）や、指定した2チャンクの一方が
     他方の内容を独立に含んでいるケース（M-01：chunk14の全文が実はchunk21の内容も含んでいた）を
     検出できていなかった。
  2. ゴールド項目のA役回答の`notes`フィールドに「gold-set construction: hand-authored A answer,
     not live web search.」等の**構築メタ情報をそのまま書いており**、`build_fact_check_prompt`が
     `AgentAnswer.model_dump()`で全フィールドをDプロンプトへダンプするため、それがそのままDに
     渡って「これは未検証の情報源」と読める形になり、`missing_knowledge`全6件に`invalid_A`側への
     不当なバイアスを作っていた。
- **根因**：(1) は2026-08-02に書いた「語一致プローブは言い換えを検出できない」という教訓を、
  今回は自分が作る側（ゴールド構成ロジック）で再現していた——測定器を検証する道具にも同じ種類の
  限界が紛れ込むことを想定していなかった。(2) は「監査用に構築過程を書き残す」つもりで書いた
  文字列が、実際にはDへの入力経路（`AgentAnswer.model_dump()`）を経由することを確認しなかった。
- **教訓**：**測定器を検証する道具（ゴールドセット生成）自体にも、測定対象と同じ種類の欠陥
  （言い換え非検出・弱い証拠の誤用）が入り込みうる。過去の教訓は「測定対象」だけでなく
  「測定器・検証器」にも同じ強度で適用すること。** また、**構造体のどのフィールドがプロンプトへ
  流れるかを、書き込む前に必ず確認する**——「これは内部向けメモ」のつもりで書いた文字列でも、
  `model_dump()`のような全フィールドダンプ経路があれば意図せず判定対象に漏れる。
  修正：`_leaks_into`（語重複ベースのパラフレーズ検出、`src/quality/classifier_gold.py`）を追加し、
  構築メタ情報をnotesから除去した上でゴールドセットを再生成（27件・新規回帰テストで固定）。

## 2026-08-03 「多段推論設問」と「真のchunking_failure」を混同したゴールドラベルを作った

- **事象**：`chunking_failure`（1事実がチャンク境界で分断され、どちらのチャンクにも完全な事実が
  無い状態）の構成的ゴールド項目5件を、`medium_multi_chunk`層（2チャンクにまたがる複合質問）から
  作った。D役サブエージェント9体による判定では、この5件**全て**が一貫して`generation_failure`
  （chunking_failureではなく）と判定された。判定理由を確認すると、いずれも「設問の各半分が
  それぞれ単独の既存チャンクで完全に答えられる」ことをDが正しく発見しており、taxonomy定義
  （「複数チャンクを合わせて初めて答えられ、単独では答えられない」）に照らせば**Dの判定が正しく、
  ゴールドラベルの側が間違っていた**。
- **根因**：`chunking_failure`の本来の定義（`src/coverage_loop.py`のコメント：「分割が悪く情報が
  分断されている＝ingest側の問題」）は、1つの**事実**がチャンク境界で寸断される状態を指す。
  一方、`medium_multi_chunk`層は「2つの**独立した事実**を組み合わせないと答えられない複合質問
  （多段推論）」であり、各事実自体はそれぞれ単独チャンクで完結していることが多い——これは
  ingestの分断とは別の現象（検索・生成の合成能力の問題）なのに、両者を同じラベルとして
  構成してしまった。
- **教訓**：**taxonomy のラベル定義を、構成に使うデータ層の性質と突き合わせて確認してから
  ゴールドセットを作る。** 「2チャンクにまたがる」という表面的な類似性だけで、
  「chunking_failure（分断）」と「多段推論（合成）」という異なる現象を同一視しないこと。
  5件全てが同じ方向に一貫して外れたことは、ノイズではなく構成方法自体の系統的な誤りを
  示すシグナルとして扱うべき——1〜2件のズレなら測定誤差を疑うが、**全件が同じ理由で
  同じ方向に倒れたら、まず自分の前提を疑う。**
  `chunking_failure`ラベルは現時点で未検証のまま。真のゴールドセットを作るには、ingestが
  実際に事実を分断した実例を探すか、意図的にchunk_size未満の位置に境界を作る構成方法が必要
  （`docs/handoff.md`③に引き継ぎ）。

## 2026-08-03 台帳の状態遷移メソッドで「IDを受け取ったら出自を検証せず信用した」

- **事象**：Phase 2でループを閉じる実装（`auto_classified → auto_approved → implemented →
  verified → active`）を作った際、`mark_coverage_candidate_implemented(candidate_id, *,
  revision_id)`が、渡された`revision_id`が本当にその候補から作られたリビジョンかを一切検証
  していなかった。独立エージェントの敵対検証がPoCで実証：無関係な、既に別の理由で検証済みの
  「野良」リビジョンをこのメソッドに直接渡すと、候補が`implemented`扱いになり、その後
  `verify_coverage_candidate`（野良リビジョン自身の正当なvalidation-jobを見つけて通過）→
  `activate_coverage_candidate`まで素通りし、**候補が提案した内容が一度もどのリビジョンにも
  現れないまま、無関係なリビジョンが本番のアクティブなナレッジベースになった**。
- **根因**：「このIDは正しい呼び出し元から来たはずだ」という暗黙の信頼で実装した。
  `approve_revision`がハッシュ照合で「このリビジョンは本当に検証されたものか」を機械的に
  確認しているのと同じ発想が、「この候補は本当にこのリビジョンを提案したのか」という
  逆方向の確認では抜けていた。
- **教訓**：**IDを受け取って別のレコードと紐付ける操作は、そのIDの出自を機械的に検証してから
  書き込む。** 「呼び出し元は正しい値を渡すはず」という前提は、API層の入力検証と同じ理由で
  信用しない。修正：リビジョン作成時（`implement_coverage_candidate`）に
  `config.coverage_candidate_id`を刻印し、紐付け時（`mark_coverage_candidate_implemented`）に
  一致を検証する形にした（`src/quality/store.py`・`src/quality/workbench.py`）。

## 2026-08-03 既存の厳格な経路に処理を委譲したが、渡す引数を既存呼び出し元と揃えなかった

- **事象**：`activate_coverage_candidate`は「別の弱い経路を作らない」という意図で、新しい
  活性化ロジックを自作せず既存の`approve_revision`（検証済みの活性化メカニズム）に委譲する
  設計にした。しかし委譲する際、標準の`/workbench/revisions/{id}/approve`エンドポイントが
  常に渡している`engine_fingerprint`/`structured_digest`の2引数を渡し忘れた。
  `approve_revision`はこの2つが`None`のとき該当チェックを**無条件でスキップする**設計
  （既存の他の呼び出し元がこの挙動を意図的に使っている場面もある）ため、エラーにも
  ならず静かに素通りした。独立エージェントの敵対検証がPoCで実証：古いエンジン設定の下で
  検証されたリビジョンが、現在のエンジン設定と食い違っているにもかかわらず活性化できた。
- **根因**：「既存の検証済みメカニズムに委譲する」という設計判断は正しかったが、
  **委譲先の関数が受け取る引数のうち、どれが省略可能でどれが実質必須か**を確認しなかった。
  `approve_revision`のシグネチャ上は`engine_fingerprint`/`structured_digest`は共に
  `None`許容（オプショナル）だが、実際の意味は「チェックを飛ばす」であり、標準の呼び出し元は
  全員これを埋めている——省略可能であることと、省略してよいことは別。
  新しい経路を作った本人は自分のテストを通していたが、そのテストが引数を省略した既存の
  「緩い」呼び出し方をそのまま踏襲していたため、抜けに気づけなかった。
- **教訓**：**既存の関数に処理を委譲するときは、その関数の「本来の」呼び出し元（今回なら
  APIエンドポイント）が渡している引数を全て突き合わせ、省略した引数それぞれについて
  「なぜ省略してよいのか」を明示的に説明できない限り渡す。** オプショナル引数は
  「無くても動く」であって「無くても安全」ではない。修正：`activate_coverage_candidate`に
  両引数を追加し、`QualityWorkbench.activate_coverage_candidate`という薄いラッパーを新設して
  `self.fingerprint()`/`self.structured_digest(revision_id)`を自動供給する形にした
  ——将来の呼び出し元が引数を省略しようとしても、ラッパーを使う限り構造的に埋まる。

## 2026-08-03 「主張を証明するテスト」を書いたが、狙った条件を本当に発生させているか確認しなかった

- **事象**：Phase 3でexplore modeの合格経路を証明するテストを追加した際、
  「explore modeはstrict/standardと違いauthority_alignmentを要求しない」ことを証明する
  つもりで、fan（ファンの考察）を装った文言のクレームをfan-authority証拠と組み合わせた
  偽検証器を書いた。しかしこの文言がたまたま`FAN_QUALIFIER_PATTERN`（fan理論・考察等を
  示す語のパターン）に一致し、`authority_alignment`は実際には`True`と計算されていた
  ——テストは自分が証明すると謳っていた「authority_alignment=Falseでも通る」経路を
  一度も通っていなかった。独立エージェントの敵対検証が、`_release_policy`のexploreブランチに
  実際に`and authority_alignment`という回帰を注入して再実行し、対象のテストが2件とも
  依然として通ってしまうことを実演して発覚した。
- **根因**：テストのdocstring・アサーションと、フィクスチャが実際に発生させている内部状態
  （`authority_alignment`の実測値）を突き合わせなかった。「このシナリオならこの条件になる
  はず」という推測だけでテストを書き、`result["authority_alignment"]`のような中間値を
  自分でアサートして確認していなかった。同じリポジトリ内に、同種の条件（fan証拠だが
  fan-qualifier語には一致しない文言）を正しく作っている既存テスト
  （`test_word_explanation_does_not_count_as_fan_theory_label`）があったにもかかわらず、
  その文言パターンを踏襲しなかった。
- **教訓**：**「〇〇を要求しない」という否定的な主張を証明するテストは、まず〇〇が
  実際に成立しない（今回ならFalseになる）ことを中間値として直接アサートしてから、
  その上で期待する結果をアサートする。** テストが緑になることと、テストが主張どおりの
  経路を通っていることは別物——特に、ある値（`authority_alignment`）を副次的に
  Trueにしてしまう別要因（`FAN_QUALIFIER_PATTERN`との偶然の一致）がコードベースに
  存在する場合、意図せずその値を固定してしまうことがある。修正：既存の非該当文言
  （「これは公式設定の説明です。」）を使う専用テストケースに分離し、
  `authority_alignment is False`を明示的にアサートしてから`release_allowed`を確認する形にした。

## 2026-08-03 新しいキーガードを追加する際、同じ入口が他に無いか横断的に洗い出さなかった

- **事象**：Phase 3で`structured-extract`/`comparison-jobs`/`validation-jobs`の
  キーガードを503に統一する作業をした際、この3エンドポイントだけに注意が向き、
  既存の`/coverage-loop`エンドポイントの既存ガード
  （`allow_llm_agents or run_knowledge_answerer`）が実は不完全だったことを見落とした。
  `run_coverage_loop`（`src/coverage_loop.py`）はラウンド2以降、両フラグの値に関わらず
  必ずLLM質問生成器を呼ぶ（ラウンド0だけが呼び出し元のseed_questionsを使う設計）ため、
  両方false・`rounds>=2`・キー未設定の組み合わせでは生のプロバイダエラーによる500が
  発生していた——このセッション自身が確立した「503+明示メッセージ」の基準から外れる、
  既存の穴。独立エージェントの敵対検証に「同じ標準をリポジトリ全体に横断適用して
  漏れが無いか」を確認させたところ、実際にPOSTして500を再現して発覚した。
- **根因**：作業スコープを「今回追加する3エンドポイント」に限定してしまい、
  「同じ標準（キー未設定なら503）を主張するなら、既存の類似コードにも同じ抜けが
  無いか」という横断確認をしなかった。既存の`/coverage-loop`ガードのコードは見ていたが、
  「フラグを見ている＝正しい」と判断し、そのフラグが実際にガードすべき全ての内部呼び出し
  （`rounds`ループの2周目以降）をカバーしているかまでは追わなかった。
- **教訓**：**「〇〇という標準を確立する」作業では、新規追加箇所だけでなく、
  同種の判断が必要な既存箇所を意図的に横断検索する。** 特に「フラグAが立っていれば
  LLM呼び出しをガードする」という設計をレビューするときは、そのフラグを見ずに
  LLM呼び出しに到達する経路が他に無いかを、フラグの参照箇所からではなく
  **LLM呼び出し本体（`question_generator.generate`等）から逆算して**確認する。
  修正：ガード条件に`request.rounds > 1`を追加し、回帰テストで固定した。
